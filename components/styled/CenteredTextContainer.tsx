import styled from "styled-components";
import Container from "./Container";

const CenteredTextContainer = styled(Container)`
  text-align: center;
  margin: auto;
`;

export default CenteredTextContainer;
